# Copy to .env for local overrides, or set as an environment variable
# in your hosting platform (Vercel/Netlify) for the deployed build.
# Leave unset locally to default to http://127.0.0.1:8000

VITE_API_BASE_URL=https://your-backend-url.onrender.com
